# Code Scanning Advanced Setup Configuration

# Disable default setup since we're using a custom workflow
use_advanced_setup: true

# Configure custom workflows that will be used for code scanning
name: "custom-workflows"
custom_workflows:
  - name: "Custom CodeQL Analysis"
    workflow_file: "custom-codeql.yml"